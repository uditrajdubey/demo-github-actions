#Goal - To say hello and print title name 

name: Hello

on:
  workflow_dispatch:

jobs:

  greet:
      runs-on: ubuntu-latest
      steps:
        - name: say hello 
          run: echo "Hello dosto okkk"
